config:
  target: "https://opscheckertest.livquik.com/signin"
  phases:
    - duration: 60
      arrivalRate: 20
 
scenarios:
  - name: "Retrieve data"
    flow:
      - get:
          url: "/example"
